name: test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Checkout the repository
        uses: actions/checkout@v2
        with:
          repository: https://github.com/oktambek2003/test.git
          ref: refs/heads/master
          path: test
      - uses: dart-lang/setup-dart@v1
      - run: dart pub get
      - run: dart test 